{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}
<div>
    {{#with main}}
    <main>
        <section>
            <h2>{{> title data="All skills" big=true }}</h2>
            <ul>
                {{#each skills}}
                <li>
                    {{name}}
                    {{#if point includeZero=true}} - ({{point}}) {{/if}}
                    {{#if isTop}} <sup>top</sup> {{/if}}
                    {{#unless point includeZero=true}} (don't studing) {{/unless}}
                    {{#unless point}}
                    {{#if point includeZero=true}} (In the process of studing) {{/if}}
                    {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
        <section>
            <h2>{{> title data="My hobbies" big=false }}</h2>
            <ul>
                {{#each hobbies}}
                <li>
                    {{name}}
                    {{#if isMain}}
                    -- (My favorite)
                    {{/if}}
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>
{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}